<h1>Admin::Pages#create</h1>
<p>Find me in app/views/admin/pages/create.html.erb</p>

<% form_for :page, @page, :url => {:action => "create"} do |f| %>
	<%= f.text_field :title %>
	<%= f.text_field :content, :class => 'mceEditor' %>
	<%= submit_tag 'Create' %>
<% end %>